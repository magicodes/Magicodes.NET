
@{
    ViewBag.Title = "EmptyPage";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>This is main title</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Dashboard_1", "Dashboards")">This is</a>
            </li>
            <li class="active">
                <strong>Breadcrumb</strong>
            </li>
        </ol>
    </div>
    <div class="col-sm-8">
        <div class="title-action">
            <a href="" class="btn btn-primary">This is action area</a>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="wrapper wrapper-content">
            <div class="middle-box text-center animated fadeInRightBig">
                <h3 class="font-bold">This is page content</h3>

                <div class="error-desc">
                    You can create here any grid layout you want. And any variation layout you imagine:) Check out main dashboard and other site. It use many different layout.
                    <br /><a href="#" class="btn btn-primary m-t">Dashboard</a>
                </div>

                <form action="" method="post" enctype="multipart/form-data">

                    <label for="file">Filename:</label>
                    <input type="file" name="file" id="file" />

                    <input type="submit" />
                </form>

                <table id="example" class="table table-striped" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Number</th>
                            <th>FirstName</th>
                            <th>LastName</th>
                        </tr>
                    </thead>

                    <tfoot>
                        <tr>
                            <th>Number</th>
                            <th>FirstName</th>
                            <th>LastName</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/plugins/dataTablesStyles")
}

@section Scripts {
@Scripts.Render("~/plugins/dataTables")
    <script type="text/javascript">

        var myActionUrl = '@Url.Action("SimpleJson", "Pages")';

        $(document).ready(function () {

            $('#example').dataTable({
                "bServerSide": true,
                "sAjaxSource": myActionUrl,
                "bProcessing": true,
                "aoColumns": [
                                { "sName": "Number" },
                                { "sName": "FirstName" },
                                { "sName": "LastName" }
                ]
            });

        });
    </script>
}