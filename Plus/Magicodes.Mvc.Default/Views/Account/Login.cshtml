@model Magicodes.Mvc.Default.Models.LoginViewModel

@{
    ViewBag.Title = "µÇÂ¼ | Magicodes";
}

<div class="am-g" style="margin-top:80px;">
    <div class="col-lg-6 col-md-8 col-sm-centered">

        @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "am-form", role = "form" }))
        {
            <h2>@ViewBag.Title</h2>
            <hr>
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "" })

            @Html.LabelFor(m => m.LoginName, new { @class = "" })

            @Html.TextBoxFor(m => m.LoginName, new { @class = "" })
            @Html.ValidationMessageFor(m => m.LoginName, "", new { @class = "" })
            <br>

            @Html.LabelFor(m => m.Password, new { @class = "" })

            @Html.PasswordFor(m => m.Password, new { @class = "" })
            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "" })

            <br>
            @Html.CheckBoxFor(m => m.RememberMe)
            @Html.LabelFor(m => m.RememberMe)

            <br />
            <div class="am-cf">
                <input type="submit" name="" value="µÇ Â¼" class="am-btn am-btn-primary am-btn-sm am-fl">
                <a href="/account/Register"><input type="button" name="" value="×¢  ²á" class="am-btn am-btn-default am-btn-sm am-fr"></a>
                <a href="/account/ForgotPassword"><input type="button" name="" value="Íü¼ÇÃÜÂë ^_^? " class="am-btn am-btn-default am-btn-sm am-fr"></a>
            </div>

        }
        <br />
        @Html.Partial("_ExternalLoginsListPartial", new Magicodes.Mvc.Default.Models.ExternalLoginListViewModel { ReturnUrl = ViewBag.ReturnUrl })
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}